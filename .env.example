# Example Environment File
# Copy this to .env.development, .env.staging, or .env.production and fill in your values

# API base URL - Use localhost for development, staging/prod URLs for deployed environments
EXPO_PUBLIC_API_BASE_URL=http://localhost:5000

# Supabase Project URL - Get from Supabase Dashboard → Settings → API
# Create separate projects for dev, staging, and production
EXPO_PUBLIC_SUPABASE_URL=https://[your-project].supabase.co

# Supabase Anonymous Key - Public key safe to expose in mobile app
# Get from Supabase Dashboard → Settings → API → Project API keys → anon public
EXPO_PUBLIC_SUPABASE_ANON_KEY=[your-anon-key]

# Environment name for display in app (development | staging | production)
EXPO_PUBLIC_ENVIRONMENT=development
